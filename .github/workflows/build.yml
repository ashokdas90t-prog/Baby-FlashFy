name: Build FlashFy - Single APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check input files
        run: |
          set -e

          test -f base.apk
          test -f split_config.arm64_v8a.apk
          test -f split_config.en.apk
          test -f split_config.xxhdpi.apk
          test -f bg.png

          mkdir -p work/apks
          mkdir -p output

          cp base.apk work/apks/base.apk
          cp split_config.arm64_v8a.apk work/apks/
          cp split_config.en.apk work/apks/
          cp split_config.xxhdpi.apk work/apks/

      - name: Download APKEditor
        run: |
          set -e

          curl -fL \
            https://github.com/REAndroid/APKEditor/releases/download/V1.4.9/APKEditor-1.4.9.jar \
            -o APKEditor.jar

          test -s APKEditor.jar

      - name: Merge split APKs
        run: |
          set -e

          echo "Merging original split APKs..."

          java -Xmx4g -jar APKEditor.jar m \
            -i work/apks \
            -o output/FlashFy-merged-unsigned.apk \
            -clean-meta \
            -f

          test -s output/FlashFy-merged-unsigned.apk

          echo "Merged APK created:"
          ls -lh output/FlashFy-merged-unsigned.apk

      - name: Replace flash ON image in FINAL merged APK
        run: |
          set -e

          rm -rf work/final_extract
          mkdir -p work/final_extract

          echo "Extracting FINAL merged APK..."

          cd work/final_extract
          unzip -q ../..

          # The command above extracts repository files too, so extract
          # the actual merged APK explicitly into a clean directory.
          cd ../..
          rm -rf work/final_extract
          mkdir -p work/final_extract

          unzip -q output/FlashFy-merged-unsigned.apk \
            -d work/final_extract

          test -f work/final_extract/AndroidManifest.xml

          TARGET="work/final_extract/assets/flutter_assets/assets/flash_on.png"

          echo "Checking target asset..."
          test -f "$TARGET"

          echo "Replacing FINAL APK asset..."

          cp bg.png "$TARGET"

          echo "Comparing hashes..."

          SOURCE_HASH=$(sha256sum bg.png | awk '{print $1}')
          TARGET_HASH=$(sha256sum "$TARGET" | awk '{print $1}')

          echo "Source : $SOURCE_HASH"
          echo "Target : $TARGET_HASH"

          test "$SOURCE_HASH" = "$TARGET_HASH"

          echo "Creating FINAL modified unsigned APK..."

          rm -f output/FlashFy-merged-unsigned.apk

          cd work/final_extract

          zip -q -r -0 ../../output/FlashFy-final-unsigned.apk .

          cd ../..

          test -s output/FlashFy-final-unsigned.apk

          echo "Final asset replacement confirmed."

      - name: Locate Android build tools
        run: |
          set -e

          BUILD_TOOLS=$(find "$ANDROID_HOME/build-tools" \
            -mindepth 1 \
            -maxdepth 1 \
            -type d \
            | sort -V \
            | tail -n 1)

          test -x "$BUILD_TOOLS/zipalign"
          test -x "$BUILD_TOOLS/apksigner"

          echo "BUILD_TOOLS=$BUILD_TOOLS" >> "$GITHUB_ENV"

      - name: Create signing key
        run: |
          set -e

          keytool -genkeypair \
            -v \
            -keystore flashfy.keystore \
            -alias flashfy \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass flashfy123 \
            -keypass flashfy123 \
            -dname "CN=FlashFy, OU=FlashFy, O=FlashFy, C=US"

      - name: Zipalign final APK
        run: |
          set -e

          "$BUILD_TOOLS/zipalign" \
            -f \
            -p \
            4 \
            output/FlashFy-final-unsigned.apk \
            output/FlashFy-aligned.apk

          rm -f output/FlashFy-final-unsigned.apk

      - name: Sign final APK
        run: |
          set -e

          "$BUILD_TOOLS/apksigner" sign \
            --ks flashfy.keystore \
            --ks-key-alias flashfy \
            --ks-pass pass:flashfy123 \
            --key-pass pass:flashfy123 \
            --out output/FlashFy-universal.apk \
            output/FlashFy-aligned.apk

          rm -f output/FlashFy-aligned.apk

      - name: Verify final APK and image
        run: |
          set -e

          echo "Verifying APK signature..."

          "$BUILD_TOOLS/apksigner" verify \
            --verbose \
            output/FlashFy-universal.apk

          echo "Checking manifest..."

          unzip -l output/FlashFy-universal.apk \
            | grep -F "AndroidManifest.xml"

          echo "Checking flash_on.png..."

          unzip -l output/FlashFy-universal.apk \
            | grep -F "assets/flutter_assets/assets/flash_on.png"

          rm -rf work/verify
          mkdir -p work/verify

          unzip -q output/FlashFy-universal.apk -d work/verify

          SOURCE_HASH=$(sha256sum bg.png | awk '{print $1}')
          FINAL_HASH=$(sha256sum \
            work/verify/assets/flutter_assets/assets/flash_on.png \
            | awk '{print $1}')

          echo "bg.png hash       : $SOURCE_HASH"
          echo "final asset hash  : $FINAL_HASH"

          test "$SOURCE_HASH" = "$FINAL_HASH"

          echo "======================================"
          echo "SUCCESS"
          echo "Final APK contains the supplied image."
          echo "======================================"

          ls -lh output/FlashFy-universal.apk

      - name: Upload final APK
        uses: actions/upload-artifact@v4
        with:
          name: FlashFy-single-APK
          path: output/FlashFy-universal.apk
          if-no-files-found: error
